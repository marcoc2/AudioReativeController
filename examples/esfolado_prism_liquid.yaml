# Esfolado — "Prism liquid" (teste): o vidro líquido (shaders/shadertoy/prism_liquid: Buffer A = a
# cena com refração, Image = desfoque tilt-shift) como está, no refrão, sem ligação com a música.
#
#   python clip_generator.py --file input/esfolado/esfolado_mix_20_09_2026.mp3 \
#       --midi input/esfolado/esfolado.mid --midi-offset 0.018 \
#       --scene examples/esfolado_prism_liquid.yaml --start-time 13.351 --bars 9 \
#       --resolution 1280x720 --fps 30 --output render_output/esfolado_prism_liquid_c6-14.mp4
video:
  layers:
    - source: shadertoy
      file: shaders/shadertoy/prism_liquid
